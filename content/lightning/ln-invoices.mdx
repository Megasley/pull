---
title: Invoices
description: "Create, decode, and settle BOLT11 invoices on testnet or regtest."
difficulty: intermediate
duration: 5 days
objectives:
  - Decode BOLT11 fields that wallets rely on
  - Create and pay invoices on a local Lightning lab
  - "Handle expiry, amounts, and description safely"
resources:
  - title: BOLT11 Invoice Protocol
    url: "https://github.com/lightning/bolts/blob/master/11-payment-encoding.md"
    kind: bolt
    required: true
  - title: Mastering the Lightning Network
    url: "https://github.com/lnbook/lnbook/blob/master/15_payment_requests.asciidoc"
    kind: book
    chapter: "Ch. 15: Payment Requests"
  - title: Polar
    url: "https://lightningpolar.com/"
    kind: tool
reflectionPrompts:
  - Explain Invoices to a teammate without jargon - what problem does it solve?
  - What would break in production if you misunderstood Invoices?
  - >-
    Which BOLT (or implementation doc) is the source of truth for this topic,
    and what did you verify there?
lab:
  title: Create + decode invoice
  description: >-
    Complete Steps 2–3. On Polar/CLN/LND lab, create an invoice, decode it, and
    identify hash, amount, and expiry fields.
  evidence:
    - Create + decode command/UI output (or screenshots)
    - Labeled fields: payment hash (or secret), amount, expiry
    - Link to BOLT11 section you used
searchQueries:
  - BOLT11
  - Lightning invoice
---

A **BOLT11 invoice** is a bech32-encoded payment request. It tells a payer what to pay, where hints might help, and when the request expires.

Complete **Step 1 // Required reading** in the study plan above, then continue here.

## Step 2 — Core idea: fields, expiry, uniqueness

POS apps, SaaS billing, and wallets all mint invoices. Incorrect expiry or amount handling causes failed payments and angry users.

**Analogy:** A temporary ticket — amount, destination commitment (payment hash), and a deadline. After expiry, the ticket is useless even if someone pays late.

Key fields:

- Payment hash / payment secret
- Amount (optional for “any amount”)
- Expiry / timestamp
- Description or description hash
- Routing hints for unannounced channels

<Mermaid
  caption="Invoice commits to a payment hash; settlement reveals the preimage."
  chart="sequenceDiagram
    participant Payee
    participant Payer
    Payee->>Payee: Create invoice (hash of preimage)
    Payee->>Payer: BOLT11 string / QR
    Payer->>Payee: Route HTLCs
    Payee->>Payer: Reveal preimage on settle"
/>

Invoices can omit amount (payer chooses) — receivers must still validate paid amounts against app expectations. Long descriptions can be **hashed** into the invoice to keep QRs small; full text travels out of band.

Show countdowns. Disable Pay on expiry. When regenerating, rotate payment hashes — never recycle.

<Callout type="warning" title="Never reuse preimages carelessly">
  Payment hashes must be unique per payment intent. Reuse can create serious
  security/privacy issues.
</Callout>

**Done when:** You can list the fields a payer UI must show before Pay is enabled.

## Step 3 — Try it: create + decode (lab)

```bash
# CLN example (Polar or local)
lightning-cli invoice 10000 "coffee" "cup #42"
lightning-cli decodepay <bolt11>
```

Label hash, amount, and expiry in the decode output. Prefer Polar if you are not running a node yet.

### Common mistakes

- Showing expired invoices in UI
- Ignoring `msat` precision vs display rounding
- Forgetting routing hints for private channels

**Done when:** Lab evidence (create/decode + labeled fields + BOLT11 link) is complete.

### Next lesson

**BOLT12 Offers** — reusable payment UX beyond one-shot invoices.
