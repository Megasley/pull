---
title: Routing
description: "Liquidity-aware path selection using fee budgets, channel hints, and reliability scores."
difficulty: advanced
duration: 6 days
objectives:
  - Explain source-based routing at a high level
  - "Factor fees, CLTV, and capacity into route choice"
  - Use explorers to build intuition for public graph structure
resources:
  - title: Mastering the Lightning Network
    url: "https://github.com/lnbook/lnbook/blob/master/08_routing_htlcs.asciidoc"
    kind: book
    chapter: "Ch. 8: Routing on the Lightning Network"
    required: true
  - title: Amboss
    url: "https://amboss.space/"
    kind: tool
  - title: LND Mission Control
    url: "https://docs.lightning.engineering/"
    kind: docs
reflectionPrompts:
  - Explain Routing to a teammate without jargon - what problem does it solve?
  - What would break in production if you misunderstood Routing?
  - >-
    Which BOLT (or implementation doc) is the source of truth for this topic,
    and what did you verify there?
lab:
  title: Path factors worksheet
  description: >-
    Complete Steps 2–3. On Polar, attempt a multi-hop pay (or inspect a failed
    attempt). List fee, CLTV, and liquidity factors that mattered. Optionally
    skim Amboss for one public path intuition.
  evidence:
    - Pay attempt result (success or failure) from Polar/CLI
    - Short list: fees, CLTV/capacity, reliability — which dominated?
    - Link to Ch. 8 section you used
searchQueries:
  - Lightning routing
  - fee budget
  - path selection
  - Mission Control
---

**Routing is liquidity-aware path selection.** The payer (usually) constructs a path across the public graph plus hints, then onion-encrypts hop instructions. Every hop must have enough **outbound** on the forward edge.

Complete **Step 1 // Required reading** in the study plan above, then continue here.

## Step 2 — Core idea: cheap ≠ successful

Routing success rates define UX. A path that looks cheap on fees still fails if any hop lacks outbound. Fee-aware, reliability-aware pathfinding separates toy demos from production wallets.

**Analogy:** You plan a road trip with toll costs and bridge weight limits. If a bridge is “full” (insufficient balance), you detour — often without knowing exact traffic until you try.

<Mermaid
  caption="Build a path from gossip + hints, then send an onion."
  chart="flowchart LR
    Payer[Payer] --> G[Channel graph]
    G --> Path[Candidate path]
    Path --> Onion[Onion packet]
    Onion --> Hops[Hop1 Hop2 Hop3]
    Hops --> Payee[Payee]"
/>

Factor in: fees, CLTV budget, max HTLC / capacity, private channel hints from invoices, and learned reliability (mission-control style).

<Callout type="tip" title="Probe carefully">
  Liquidity is not fully public. Nodes learn from failures without leaking customer
  details into logs carelessly.
</Callout>

**Done when:** You can name three inputs to route choice beyond “lowest fee.”

## Step 3 — Try a path (lab)

Multi-hop pay on Polar. Note which constraint bit you if it fails. Optional Amboss skim for graph intuition.

### Common mistakes

- Optimizing only for lowest fee and ignoring success probability
- Ignoring max HTLC / capacity constraints
- Forgetting private channel hints from invoices

**Done when:** Lab evidence (attempt + factor list + source) is complete.

### Next lesson

**Onion Routing** — how hop payloads stay private.
