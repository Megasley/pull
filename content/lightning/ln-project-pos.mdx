---
title: Lightning POS
description: Build a point-of-sale app that creates invoices and tracks settlement.
difficulty: advanced
duration: 2 weeks
project: lightning-pos
objectives:
  - Create invoices from a backend node
  - Subscribe to invoice settlement events
  - Design a cashier UX for pending vs paid
resources:
  - title: "Project Spec: Lightning POS"
    url: "/projects/lightning-pos"
    kind: docs
    note: Pull project page — acceptance criteria and submit flow.
    required: true
  - title: BOLT11
    url: "https://github.com/lightning/bolts/blob/master/11-payment-encoding.md"
    kind: bolt
    required: true
  - title: LND invoices API
    url: "https://docs.lightning.engineering/"
    kind: docs
  - title: Mastering the Lightning Network
    url: "https://github.com/lnbook/lnbook/blob/master/15_payment_requests.asciidoc"
    kind: book
    chapter: "Ch. 15: Payment Requests"
reflectionPrompts:
  - >-
    Explain Lightning POS to a teammate without jargon - what problem does it
    solve?
  - What would break in production if you misunderstood Lightning POS?
  - >-
    Which BOLT (or implementation doc) is the source of truth for this topic,
    and what did you verify there?
lab:
  title: Project milestone evidence
  description: >-
    Complete Steps 2–4. Ship create invoice → QR → settle (or expire) on
    Polar/regtest and capture demo evidence before marking complete.
  evidence:
    - Link to public repo or branch
    - Screenshots or commands proving charge → paid (and expiry path)
    - Short note of one failure case (duplicate charge, late pay, node down)
searchQueries:
  - BOLT11
---

A Lightning POS turns protocol knowledge into a merchant tool: show QR, wait for payment, print/mark paid.

Complete **Step 1 // Required reading** in the study plan above, then continue here. This opens **Lightning Projects**.

## Step 2 — Goal

1. Create invoice for cart total
2. Display BOLT11 QR
3. Detect settlement via websocket/webhook/RPC subscribe
4. Handle expiry and cancel

<Mermaid
  caption="POS mints invoices and listens until settled or expired."
  chart="sequenceDiagram
    participant Cashier
    participant POS
    participant LN as Lightning node
    Cashier->>POS: Charge $X
    POS->>LN: Add invoice
    LN->>POS: bolt11
    POS->>Cashier: Show QR
    LN->>POS: Settled
    POS->>Cashier: Paid"
/>

**Done when:** You can narrate pending vs paid vs expired without opening the code.

## Step 3 — Milestones

| Phase       | Ship                                                       |
| ----------- | ---------------------------------------------------------- |
| Milestone A | Backend mint invoice + return bolt11                       |
| Milestone B | Cashier UI: QR + pending/paid states                       |
| Milestone C | Subscribe to settle; expiry/cancel; idempotent charge keys |

<Callout type="tip" title="Idempotent tickets">
  Bind each charge to an idempotency key so refreshes do not create duplicate invoices
  accidentally.
</Callout>

**Done when:** Milestone A works before you polish UI chrome.

## Step 4 — Build & evidence (lab)

Implement against `/projects/lightning-pos`. Capture repo link, happy-path demo, and one failure case.

### Common mistakes

- Creating a new invoice on every UI refresh
- Treating “invoice created” as paid
- Blocking the cashier UI on synchronous pay RPCs without a pending state

**Done when:** Lab evidence is filed.

### Next lesson

**Node Ops Dashboard** — channels, peers, balances, and routing revenue.
