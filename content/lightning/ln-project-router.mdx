---
title: Custom Router
description: Implement a pathfinding service with custom fee and reliability heuristics.
difficulty: advanced
duration: 2 weeks
project: custom-router
objectives:
  - Ingest a channel graph snapshot
  - Score edges with custom weights
  - Benchmark success vs baseline routing
resources:
  - title: 'Project Spec: Custom Router'
    url: '/projects/custom-router'
    kind: docs
    note: Pull project page — acceptance criteria and submit flow.
    required: true
  - title: Mastering the Lightning Network
    url: 'https://github.com/lnbook/lnbook/blob/master/12_path_finding.asciidoc'
    kind: book
    chapter: 'Ch. 12: Pathfinding'
    required: true
  - title: Amboss API / graph data
    url: 'https://amboss.space/'
    kind: tool
  - title: BOLT7 Gossip
    url: 'https://github.com/lightning/bolts/blob/master/07-routing-gossip.md'
    kind: bolt
reflectionPrompts:
  - >-
    Explain Custom Router to a teammate without jargon - what problem does it
    solve?
  - What would break in production if you misunderstood Custom Router?
  - >-
    Which BOLT (or implementation doc) is the source of truth for this topic,
    and what did you verify there?
lab:
  title: Project milestone evidence
  description: >-
    Complete Steps 2–4. Ship a service that returns ranked routes for
    (source, dest, amount) and capture a small benchmark note vs a baseline.
  evidence:
    - Link to public repo or branch
    - Example route response or CLI output for a sample query
    - Short benchmark note (success rate, fee, or latency vs baseline)
searchQueries:
  - BOLT7
---
A custom router lets you experiment with weights: fee, uptime, historical fail rates, and liquidity hints.

Complete **Step 1 // Required reading** in the study plan above, then continue here. This closes **Lightning Projects**.

## Step 2 — Goal

Service that:

1. Loads graph data
2. Returns routes for (source, destination, amount)
3. Logs hypothetical success metrics against a dataset

<Mermaid caption="Score edges, search paths, measure outcomes." chart="flowchart TD
    Graph[Graph snapshot] --> Scorer[Edge scorer]
    Scorer --> Search[Path search]
    Search --> Routes[Ranked routes]
    Routes --> Bench[Benchmark harness]" />

**Done when:** You can write the edge-weight formula you will ship first.

## Step 3 — Milestones

| Phase | Ship |
| --- | --- |
| Milestone A | Ingest graph snapshot (file, Amboss, or node gossip export) |
| Milestone B | Ranked routes API for amount-constrained search |
| Milestone C | Benchmark harness vs baseline weights |

**Done when:** Milestone B returns at least one path or a clear “no route” with reasons.

## Step 4 — Build & evidence (lab)

Implement against `/projects/custom-router`. Capture repo link, sample route output, and benchmark note.

### Common mistakes

- Optimizing only fees and ignoring historical fail rates
- Treating gossip capacity as guaranteed liquidity
- Benchmarking only on a single lucky path

**Done when:** Lab evidence is filed.

### Next lesson

**Contribution Discovery** — find beginner-friendly issues across LND, CLN, and LDK.
