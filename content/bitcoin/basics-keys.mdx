---
title: Keys & Addresses
description: "Private keys, public keys, address encodings from P2PKH through Taproot."
difficulty: beginner
duration: 5 days
objectives:
  - Derive the mental model from private key to address
  - "Recognize legacy, SegWit, and Taproot address formats"
  - "Know which encoding standards apply (Base58, Bech32, Bech32m)"
resources:
  - title: Mastering Bitcoin
    url: "https://github.com/bitcoinbook/bitcoinbook/blob/develop/ch04_keys.adoc"
    kind: book
    chapter: "Ch. 4: Keys and Addresses"
    required: true
  - title: BIP-173 Bech32
    url: "https://github.com/bitcoin/bips/blob/master/bip-0173.mediawiki"
    kind: bip
  - title: BIP-350 Bech32m
    url: "https://github.com/bitcoin/bips/blob/master/bip-0350.mediawiki"
    kind: bip
reflectionPrompts:
  - >-
    Explain Keys & Addresses to a teammate without jargon - what problem does it
    solve?
  - What would break in production if you misunderstood Keys & Addresses?
  - >-
    Which BIP, book chapter, or Core doc is authoritative here, and what did you
    verify?
lab:
  title: Address types on regtest
  description: >-
    Complete Steps 2–3. Generate Bech32 and Bech32m addresses on regtest and
    inspect scriptPubKey / descriptor fields with getaddressinfo.
  evidence:
    - "getnewaddress output for bech32 and bech32m (or equivalent)"
    - getaddressinfo snippet showing scriptPubKey or desc for one address
    - One sentence linking the address string to the locking condition
searchQueries:
  - BIP32
  - BIP39
---

An address is not a bank account number in a central database. It is a compact encoding of a **locking condition** — usually “prove you control this public key.”

Complete **Step 1 // Required reading** in the study plan above, then continue here.

## Step 2 — Core idea: private → public → address

Wrong address type, wrong network prefix, or a typo can permanently lose funds. Validate addresses and know what each format commits to.

**Analogy:** The **private key** is the only key to a mailbox. The **public key** is a lock design others can verify. The **address** is the mailbox label so people can send mail without seeing the lock’s internals.

<Mermaid
  caption="Derivation is one-way: addresses do not reveal private keys."
  chart="flowchart LR
    Priv[Private key] --> Pub[Public key]
    Pub --> Hash[Hash160 /<br/>tweaked key]
    Hash --> Addr[Address]"
/>

```text
private key  →  public key  →  hashing / tweaking  →  address string
```

Never type private keys into random websites. In apps, keep keys in secure modules or hardware wallets.

| Type           | Looks like      | Notes               |
| -------------- | --------------- | ------------------- |
| P2PKH          | starts with `1` | Legacy Base58Check  |
| P2SH           | starts with `3` | Script hash wrapper |
| P2WPKH / P2WSH | `bc1q...`       | SegWit v0 Bech32    |
| P2TR           | `bc1p...`       | Taproot Bech32m     |

Testnet/regtest use different HRPs (`tb1`, `bcrt1`, etc.). Always check the network.

<Mermaid
  caption="Different output types encode different locking conditions."
  chart="flowchart TD
    Priv[Private key] --> Pub[Public key]
    Pub --> P2PKH[P2PKH / P2WPKH]
    Pub --> P2TR[Taproot output key]
    Script[Script] --> P2SH[P2SH / P2WSH]
    P2PKH --> Addr[Address string]
    P2TR --> Addr
    P2SH --> Addr"
/>

<Callout type="warning" title="Checksums are not enough">
  Address checksums catch typos, but they do not stop you from sending to the wrong
  person or the wrong network.
</Callout>

**Base58Check** (legacy) mixes numbers and letters and appends a checksum. **Bech32** has stronger error detection; **Bech32m** (BIP-350) tweaks the checksum for Taproot (`bc1p`). If a library says “invalid checksum,” regenerate — do not hand-edit the string.

HD wallets derive many keys from one seed. Back up the seed/mnemonic, the descriptor or derivation standard, and multisig cosigner records if applicable.

**Done when:** You can map each address family in the table to an encoding and network HRP.

## Step 3 — Try it on regtest (lab)

```bash
bitcoin-cli -regtest getnewaddress "" "bech32"
bitcoin-cli -regtest getnewaddress "" "bech32m"
bitcoin-cli -regtest getaddressinfo <addr>
```

Read `scriptPubKey` and `desc` — they connect the address string to the underlying lock.

### Builder habits

- Validate with a library, not regex alone
- Refuse sends to the wrong chain
- Prefer Bech32/Bech32m for new wallets (fees + error detection)

**Done when:** Lab evidence (two address types + getaddressinfo + one linking sentence) is complete.

### Next lesson

**Blocks & Chain** — where confirmed UTXOs live and how reorgs affect finality.
