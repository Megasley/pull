---
title: Descriptors & Miniscript
description: Express wallet policies as output descriptors and compose safe scripts.
difficulty: intermediate
duration: 6 days
objectives:
  - Read and write basic output descriptors
  - Explain why descriptors beat ad-hoc path assumptions
  - Use Miniscript as a policy language mindset
resources:
  - title: BIP-380 Output Script Descriptors
    url: 'https://github.com/bitcoin/bips/blob/master/bip-0380.mediawiki'
    kind: bip
    required: true
  - title: Miniscript
    url: 'https://bitcoin.sipa.be/miniscript/'
    kind: docs
  - title: Bitcoin Core descriptors
    url: 'https://github.com/bitcoin/bitcoin/blob/master/doc/descriptors.md'
    kind: docs
reflectionPrompts:
  - >-
    Explain Descriptors & Miniscript to a teammate without jargon - what problem
    does it solve?
  - >-
    What would break in production if you misunderstood Descriptors &
    Miniscript?
  - >-
    Which BIP, book chapter, or Core doc is authoritative here, and what did you
    verify?
lab:
  title: Decode a descriptor
  description: >-
    Complete Steps 2–3. On regtest, run getdescriptorinfo on a sample
    descriptor (or listdescriptors from a lab wallet) and explain receive vs
    change in one sentence.
  evidence:
    - getdescriptorinfo (or listdescriptors) output excerpt
    - One sentence on what the descriptor commits to (wpkh/tr/multi…)
    - Link to BIP-380 or Core descriptors doc you used
searchQueries:
  - BIP380
  - output descriptors
---
**Output descriptors** are a standard way to describe what scripts/keys a wallet controls. **Miniscript** helps compile human policies into safe Script fragments.

Complete **Step 1 // Required reading** in the study plan above, then continue here.

## Step 2 — Core idea: portable policy, not path folklore

“m/84'/0'/0'” alone is incomplete if you forget whether you meant wpkh, tr, multisig, or a custom tree. Descriptors make imports, hardware coordination, and recoveries unambiguous.

**Analogy:** A descriptor is a precise parts list and assembly diagram for your locks. Miniscript is a grammar that prevents you from building a lock that jams shut forever.

```text
wpkh([fingerprint/84h/1h/0h]tpub.../0/*)
tr([fingerprint/86h/1h/0h]tpub.../0/*)
wsh(multi(2,key1,key2,key3))
```

Bitcoin Core RPCs like `importdescriptors` and `listdescriptors` operationalize this.

<Mermaid caption="Policies compile to scripts wrapped by portable descriptors." chart="flowchart LR
    Policy[Spending policy] --> Mini[Miniscript]
    Mini --> Script[Script]
    Script --> Desc[Descriptor]
    Desc --> Core[Bitcoin Core / BDK]
    Desc --> Backup[Backup &amp; recovery]" />

<Callout type="tip" title="Backup descriptors with seeds">
  For Taproot script trees and multisig, seed words alone may be insufficient. Store descriptors (and multisig key records) with the same care as seeds.
</Callout>

**Done when:** You can read a `wpkh(...)` / `tr(...)` line and say what locking condition it describes.

## Step 3 — Try it: getdescriptorinfo (lab)

```bash
bitcoin-cli -regtest getdescriptorinfo "wpkh(tpubD6NzVbkrYhZ4XgiXtGrdW5XDAPFCL9h7we1vwNCpn8tZpoxq8n3nS8/0/*)"
# Or after creating a wallet:
bitcoin-cli -regtest listdescriptors
```

(Use a throwaway/example key material — never paste real mainnet xpubs into shared notes.)

### Common mistakes

- Exporting xpubs without origin info / fingerprints
- Mixing receive and change descriptor chains
- Assuming Miniscript supports every opcode policy you can imagine

**Done when:** Lab evidence (RPC excerpt + commitment sentence + source link) is complete.

### Next lesson

**Bitcoin Core & RPC** — drive the reference node for wallets, chain, and tests.
