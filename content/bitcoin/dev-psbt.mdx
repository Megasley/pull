---
title: PSBT Workflows
description: Coordinate multi-party signing with Partially Signed Bitcoin Transactions.
difficulty: intermediate
duration: 6 days
objectives:
  - 'Explain the PSBT roles: creator, updater, signer, finalizer, extractor'
  - Carry a PSBT across wallet and hardware boundaries
  - Know when to use PSBTv0 vs later extensions conceptually
resources:
  - title: BIP-174 PSBT
    url: 'https://github.com/bitcoin/bips/blob/master/bip-0174.mediawiki'
    kind: bip
    required: true
  - title: Bitcoin Core PSBT RPCs
    url: 'https://developer.bitcoin.org/reference/rpc/#blockchain-rpcs'
    kind: docs
    note: 'See walletcreatefundedpsbt, walletprocesspsbt, finalizepsbt, send.'
  - title: 'Bitcoin Optech: PSBT'
    url: 'https://bitcoinops.org/en/topics/psbt/'
    kind: article
reflectionPrompts:
  - >-
    Explain PSBT Workflows to a teammate without jargon - what problem does it
    solve?
  - What would break in production if you misunderstood PSBT Workflows?
  - >-
    Which BIP, book chapter, or Core doc is authoritative here, and what did you
    verify?
lab:
  title: Funded PSBT on regtest
  description: >-
    Complete Steps 2–3. On regtest, create a funded PSBT to a lab address,
    process/sign it with the wallet, finalize, and note the role each RPC
    played.
  evidence:
    - walletcreatefundedpsbt (or equivalent) output excerpt
    - walletprocesspsbt / finalizepsbt success fields (or decoded summary)
    - One sentence mapping creator → signer → finalizer for your flow
    - Link to BIP-174 or Optech PSBT section you used
searchQueries:
  - BIP174
  - PSBT
---
**PSBT** (Partially Signed Bitcoin Transaction) is a standard container for incomplete transactions plus the metadata signers need (UTXO info, paths, scripts, partial signatures).

Complete **Step 1 // Required reading** in the study plan above, then continue here. This closes **Development**.

## Step 2 — Core idea: roles and interoperability

Hardware wallets, multisig cosigners, and watch-only constructors should not invent proprietary blob formats. PSBT is the interoperability layer.

**Analogy:** A shared clipboard for a group project — one person drafts, others add signatures in the right places, someone finalizes, then you submit.

<Mermaid caption="PSBT moves through roles until signatures are complete and extractable." chart="flowchart LR
    C[Creator] --> U[Updater]
    U --> S1[Signer A]
    S1 --> S2[Signer B]
    S2 --> F[Finalizer]
    F --> E[Extractor / broadcast]" />

<Callout type="info" title="Include witness UTXO data">
  Signers need previous output information. Incomplete PSBTs fail mysteriously on hardware devices when UTXO data is missing.
</Callout>

**Done when:** You can name the PSBT roles and why watch-only + hardware needs this format.

## Step 3 — Try it: create → process → finalize (lab)

Fund a wallet first if needed, then:

```bash
ADDR=$(bitcoin-cli -regtest getnewaddress)
bitcoin-cli -regtest walletcreatefundedpsbt '[]' "{\"$ADDR\":0.01}"
# Then walletprocesspsbt <psbt> and finalizepsbt <psbt>
```

Map which command acted as creator, signer, and finalizer in your run.

### Common mistakes

- Asking hardware to sign without verifying outputs on the device screen
- Mixing finalized and unfinalized blobs in your database
- Forgetting change outputs belong to your descriptor

**Done when:** Lab evidence (PSBT RPC excerpts + role sentence + source) is complete. Complete the **chapter quiz** when prompted.

### Next lesson

**Mini Wallet Project** — apply UTXOs, descriptors, regtest, and PSBT in one CLI.
