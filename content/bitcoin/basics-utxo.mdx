---
title: UTXO Model
description: 'Understand inputs, outputs, and how wallets track spendable balances.'
difficulty: beginner
duration: 4 days
objectives:
  - Explain UTXOs versus account balances
  - Trace how inputs consume outputs and create new ones
  - Describe coin selection at a high level
resources:
  - title: Mastering Bitcoin
    url: >-
      https://github.com/bitcoinbook/bitcoinbook/blob/develop/ch06_transactions.adoc
    kind: book
    chapter: 'Ch. 6: Transactions'
    note: 'Focus on inputs, outputs, and how value moves.'
    required: true
  - title: 'Decoding Bitcoin: UTXO'
    url: 'https://bitcoindevs.xyz/decoding/utxo'
    kind: interactive
    note: Interactive explainer - contrast UTXO vs account before the lab.
    required: true
  - title: 'Decoding Bitcoin: Transaction structure'
    url: 'https://bitcoindevs.xyz/decoding/transaction-structure'
    kind: interactive
    note: See how outputs become the next spendable UTXOs.
  - title: Mempool.space
    url: 'https://mempool.space/'
    kind: tool
  - title: 'Bitcoin Optech: Coin selection'
    url: 'https://bitcoinops.org/en/topics/coin-selection/'
    kind: article
reflectionPrompts:
  - >-
    Explain UTXO Model to a teammate without jargon - what problem does it
    solve?
  - What would break in production if you misunderstood UTXO Model?
  - >-
    Which BIP, book chapter, or Core doc is authoritative here, and what did you
    verify?
lab:
  title: Trace a UTXO spend
  description: >-
    Complete Steps 2–3. Finish the Decoding Bitcoin UTXO module, then on
    regtest list unspent outputs, spend one, and show the new change/payment
    UTXOs created.
  evidence:
    - Note or screenshot from the Decoding Bitcoin UTXO interactive section
    - 'Before/after listunspent (or wallet UTXO view) from regtest'
    - One sentence explaining which output was consumed and which new locks appeared
---
Bitcoin does not keep a global balance per address. It tracks **unspent transaction outputs (UTXOs)** — discrete chunks of value that can be spent exactly once.

Complete **Step 1 // Required reading** in the study plan above, then continue here. Use your Foundations **regtest** node for the hands-on step.

## Step 2 — Core idea: inputs, outputs, change

If you think in bank-style “account balance” terms, you will mis-build wallets, explorers, and Lightning funding logic. UTXOs are the native accounting unit.

**Analogy:** Cash bills in a wallet. You do not subtract $7 from a $20 balance in place — you hand over bills and get change. Each bill is a UTXO.

A transaction:

1. **Consumes** one or more UTXOs as inputs
2. **Creates** one or more new outputs
3. Pays the difference as a miner fee (no explicit fee output)

<Mermaid caption="Inputs are fully consumed; change is a new UTXO; the remainder is the fee." chart="flowchart LR
    U1[Input A<br/>0.05 BTC] --> Tx[Transaction]
    U2[Input B<br/>0.02 BTC] --> Tx
    Tx --> Pay[Payment<br/>0.06 BTC]
    Tx --> Chg[Change<br/>0.009 BTC]
    Tx --> Fee[Fee 0.001 BTC]" />

```typescript
type Utxo = {
  txid: string;
  vout: number;
  valueSats: number;
  scriptPubKey: string;
};

const balance = utxos.reduce((sum, u) => sum + u.valueSats, 0);
```

Wallet balance is the sum of UTXOs your keys can unlock — not a single number stored on-chain.

When sending, wallets **select coins**. Goals often include minimizing fees, avoiding unnecessary change, and preserving privacy when possible.

<Callout type="info" title="No partial spends">
  Each input is fully consumed. There is no “spend half of this UTXO” without creating change.
</Callout>

**Done when:** You can explain fee = inputs − outputs and why change is a new UTXO.

## Step 3 — Try it: count and spend UTXOs (lab)

Complete the Decoding Bitcoin UTXO interactive, then on regtest:

```bash
ADDR=$(bitcoin-cli -regtest getnewaddress)
bitcoin-cli -regtest generatetoaddress 101 "$ADDR"
bitcoin-cli -regtest listunspent
```

Each `listunspent` row is a UTXO (`txid`, `vout`, `amount`). Send, then list again — some rows disappear and new ones appear (payment + change).

Very small UTXOs can cost more in fees to spend than they are worth (**dust**). Consolidation improves future fee efficiency but links history (privacy tradeoff).

### Common mistakes

- Forgetting change and accidentally overpaying the fee
- Creating dust that costs more to spend later than it is worth
- Assuming address-balance APIs equal UTXO-aware wallet state

**Done when:** Lab evidence (Decoding note + before/after listunspent + one-sentence spend story) is complete.

### Next lesson

**Keys & Addresses** — how private keys become the locking conditions on those UTXOs.
