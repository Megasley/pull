---
title: Bitcoin Script
description: "Learn script puzzles, locking conditions, and standard output types."
difficulty: intermediate
duration: 7 days
objectives:
  - Explain locking vs unlocking scripts
  - Walk the standard output ladder (P2PK → P2PKH → P2SH → P2MS → SegWit/Taproot)
  - "Reason about Script as a predicate, not a general programming language"
resources:
  - title: Mastering Bitcoin
    url: >-
      https://github.com/bitcoinbook/bitcoinbook/blob/develop/ch07_authorization-authentication.adoc
    kind: book
    chapter: "Ch. 7: Authorization and Authentication"
    required: true
  - title: "Decoding Bitcoin: Locking & unlocking"
    url: "https://bitcoindevs.xyz/decoding/locking-unlocking"
    kind: interactive
    note: Start here - scriptPubKey vs scriptSig/witness mental model.
    required: true
  - title: "Decoding Bitcoin: P2PK"
    url: "https://bitcoindevs.xyz/decoding/p2pk"
    kind: interactive
    note: "Ladder step 1 - pay to public key (and why wallets moved on)."
    required: true
  - title: "Decoding Bitcoin: P2PKH"
    url: "https://bitcoindevs.xyz/decoding/p2pkh"
    kind: interactive
    note: "Ladder step 2 - hash the key; complete at least one P2PKH exercise."
    required: true
  - title: "Decoding Bitcoin: P2SH"
    url: "https://bitcoindevs.xyz/decoding/p2sh"
    kind: interactive
    note: "Ladder step 3 - hide redeem scripts until spend (BIP16)."
  - title: "Decoding Bitcoin: P2MS"
    url: "https://bitcoindevs.xyz/decoding/p2ms"
    kind: interactive
    note: "Ladder step 4 - bare multisig vs P2SH-wrapped policy."
  - title: "Decoding Bitcoin: Build a Bitcoin stack"
    url: "https://bitcoindevs.xyz/decoding/project"
    kind: interactive
    note: Capstone mini-project - stack execution before descriptors/Miniscript.
  - title: Script opcodes reference
    url: "https://en.bitcoin.it/wiki/Script"
    kind: docs
  - title: Miniscript
    url: "https://bitcoin.sipa.be/miniscript/"
    kind: docs
reflectionPrompts:
  - >-
    Explain Bitcoin Script to a teammate without jargon - what problem does it
    solve?
  - What would break in production if you misunderstood Bitcoin Script?
  - >-
    Walk P2PK → P2PKH → P2SH in one paragraph: what problem did each step solve?
lab:
  title: Script ladder + stack exercise
  description: >-
    Complete Steps 2–3. Finish the Decoding Bitcoin output-type ladder through
    P2PKH (required), then either one P2SH/P2MS exercise or the stack project.
    Optionally verify a matching template on regtest with decodescript /
    descriptors.
  evidence:
    - Link or screenshot from a completed Decoding Bitcoin P2PKH exercise
    - Evidence from one further ladder step (P2SH, P2MS, or stack project)
    - One-paragraph comparison of locking vs unlocking for the template you studied
    - "Optional: decodescript / descriptor output from regtest matching that template"
searchQueries:
  - Bitcoin script
  - P2PKH
  - P2SH
  - Miniscript
---

**Bitcoin Script** is the stack-based language that defines spending conditions. It is intentionally limited — not Turing-complete — so validation stays predictable and safe for nodes.

Complete **Step 1 // Required reading** in the study plan above (locking/unlocking + P2PK + P2PKH), then continue here.

## Step 2 — Core idea: lock, unlock, predicate

Wallets, multisig vaults, Lightning commitments, and covenant proposals all express rules in Script (or Tapscript). Misreading a scriptPubKey means misunderstanding who can spend coins.

**Analogy:** A locking script is a riddle on a locked box. The unlocking script (scriptSig/witness) is the answer. Nodes run both and check that the riddle evaluates to true.

When spending:

1. Take the previous output’s `scriptPubKey` (lock)
2. Provide `scriptSig` / witness (unlock)
3. Execute according to consensus rules

<Mermaid
  caption="Spends succeed only when the unlocking data satisfies the lock."
  chart="flowchart LR
    Lock[scriptPubKey lock] --> Combine[Combine with unlock]
    Unlock[scriptSig / witness] --> Combine
    Combine --> Exec[Script execution]
    Exec --> Yes[Valid spend]
    Exec --> No[Invalid]"
/>

<Callout type="info" title="Script is a predicate">
  Think “does this satisfy the condition?”, not “run an app on-chain.” Loops and
  unbounded complexity are restricted for a reason.
</Callout>

Script pushes and pops values on a stack. `OP_DUP`, `OP_HASH160`, `OP_EQUALVERIFY`, and `OP_CHECKSIG` appear often in P2PKH-style templates. You rarely write these by hand today — descriptors and Miniscript generate them — but reading them debugs explorers and policy bugs.

Opcodes like `OP_CHECKLOCKTIMEVERIFY` (CLTV) and `OP_CHECKSEQUENCEVERIFY` (CSV) add time constraints. Lightning depends on these. Miniscript helps compose policies safely.

Even if a script is consensus-valid, **mempool policy** may refuse to relay non-standard forms. Test both inclusion via miner on regtest and relay approximating mainnet policy.

**Done when:** You can explain lock vs unlock and why Script is a predicate, not a general language.

## Step 3 — Climb the output-type ladder (lab)

Do not memorize every opcode first. Climb the historical ladder (Decoding Bitcoin modules in the study plan):

1. **P2PK** — pay directly to a public key (simple, leaks the key early)
2. **P2PKH** — pay to `HASH160(pubkey)`; classic `1...` / wrapped patterns
3. **P2SH** — pay to a script hash; reveal the redeem script only at spend (BIP16)
4. **P2MS** — bare multisig; usually wrapped in P2SH/P2WSH in practice
5. **SegWit / Taproot** — witness programs (`P2WPKH`, `P2WSH`, `P2TR`) — later lessons

```text
# Conceptual P2WPKH witness stack (after the ladder)
<signature> <pubkey>
```

Complete required P2PKH, then one further step (P2SH, P2MS, or stack project). Optionally:

```bash
bitcoin-cli -regtest decodescript <hex>
```

Human policy (“2-of-3 keys, or Alice alone after 90 days”) becomes a composition of multisig and timelock branches. Taproot can hide unused branches until needed.

### Common mistakes

- Hand-editing scripts without policy tests
- Confusing policy intent with exact opcode encoding
- Assuming non-standard scripts will relay on default mempool policy

**Done when:** Lab evidence (P2PKH + one further ladder step + lock/unlock paragraph) is complete.

### Next lesson

**SegWit & Bech32** — how witnesses changed fees, malleability, and address formats.
