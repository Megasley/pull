---
title: Mini Wallet Project
description: "Build a CLI wallet that generates addresses, receives, and sends on regtest."
difficulty: intermediate
duration: 2 weeks
project: mini-wallet
objectives:
  - Generate receive addresses from a descriptor or HD wallet
  - Track UTXOs and build a funded transaction on regtest
  - Sign and broadcast with tests covering change and fees
resources:
  - title: "Project Spec: Mini Wallet"
    url: "/projects/mini-wallet"
    kind: docs
    note: Pull project page — acceptance criteria and submit flow.
    required: true
  - title: Mastering Bitcoin
    url: "https://github.com/bitcoinbook/bitcoinbook/blob/develop/ch05_wallets.adoc"
    kind: book
    chapter: "Ch. 5: Wallets"
    required: true
  - title: BDK
    url: "https://bitcoindevkit.org/"
    kind: docs
reflectionPrompts:
  - >-
    Explain Mini Wallet Project to a teammate without jargon - what problem does
    it solve?
  - What would break in production if you misunderstood Mini Wallet Project?
  - >-
    Which BIP, book chapter, or Core doc is authoritative here, and what did you
    verify?
lab:
  title: Project milestone evidence
  description: >-
    Complete Steps 2–4. Ship the Mini Wallet happy path on regtest and capture
    demo evidence before marking the lesson complete. Submit via the project
    page when ready for review.
  evidence:
    - Link to public repo or branch
    - Commands or screenshots proving receive + send on regtest
    - Short note of one failure case you tested (fee, change, or sync)
---

This project turns protocol lessons into a working tool: a command-line wallet on regtest that can receive and send bitcoin.

Complete **Step 1 // Required reading** in the study plan above (project spec + wallets chapter), then continue here. This opens **Projects**.

## Step 2 — Goal

Ship a CLI that can:

1. Create or load a wallet (seed/descriptor)
2. Show a receive address and balance
3. Send to an address with an explicit fee rate
4. Mine/confirm via Bitcoin Core RPC in tests

<Mermaid
  caption="CLI drives wallet logic against a local regtest node."
  chart="flowchart LR
    CLI[CLI] --> Wallet[Wallet state]
    Wallet --> Core[bitcoind regtest]
    CLI --> Build[Build + sign tx]
    Build --> Core"
/>

**Done when:** You can describe the happy path in four bullets without opening the code.

## Step 3 — Milestones

| Phase       | Ship                                                                |
| ----------- | ------------------------------------------------------------------- |
| Milestone A | Address generation + sync/balance on regtest                        |
| Milestone B | Coin selection, change, broadcast, fee unit tests                   |
| Milestone C | Acceptance test: mine → send half → assert balances after one block |

<Callout type="tip" title="Acceptance tests first">
  Write a test that mines funds to your wallet, sends half to a second wallet, and
  asserts balances after one block.
</Callout>

**Done when:** Milestone A works end-to-end before you start fee edge cases.

## Step 4 — Build & evidence (lab)

Implement against `/projects/mini-wallet`. Capture repo link, happy-path proof, and one failure case you tested.

### Common mistakes

- Hard-coding mainnet network objects in tests
- Forgetting change and overpaying the fee
- Demo without a reproducible test command

**Done when:** Lab evidence is filed (and optionally submitted for review).

### Next lesson

**Block Explorer UI** — visualize blocks, txs, and mempool for your private chain.
