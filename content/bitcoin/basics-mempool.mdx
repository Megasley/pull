---
title: Mempool & Fees
description: "Follow unconfirmed transactions, fee estimation, RBF, and CPFP."
difficulty: intermediate
duration: 4 days
objectives:
  - Explain mempool policy versus consensus rules
  - Estimate fees and choose confirmation targets
  - Apply RBF and CPFP fee-bumping strategies
resources:
  - title: Mastering Bitcoin
    url: "https://github.com/bitcoinbook/bitcoinbook/blob/develop/ch09_fees.adoc"
    kind: book
    chapter: "Ch. 9: Transaction Fees"
    required: true
  - title: BIP-125 RBF
    url: "https://github.com/bitcoin/bips/blob/master/bip-0125.mediawiki"
    kind: bip
  - title: "Bitcoin Optech: Fee bumping"
    url: "https://bitcoinops.org/en/topics/fee-bumping/"
    kind: article
reflectionPrompts:
  - >-
    Explain Mempool & Fees to a teammate without jargon - what problem does it
    solve?
  - What would break in production if you misunderstood Mempool & Fees?
  - >-
    Which BIP, book chapter, or Core doc is authoritative here, and what did you
    verify?
lab:
  title: Fee rate + bump strategy
  description: >-
    Complete Steps 2–3. On regtest (or with estimatesmartfee), compute a
    sat/vB example and write which bump tool you would use if the tx stalled —
    RBF or CPFP — and why.
  evidence:
    - "Fee rate calculation (feeSats / vsize) or estimatesmartfee output"
    - One sentence choosing RBF vs CPFP for a stuck payment scenario
    - Link to Mastering Bitcoin Ch. 9 or Optech fee-bumping section you used
searchQueries:
  - mempool
  - BIP125
---

The **mempool** is each node’s holding area for valid unconfirmed transactions. Miners (and other nodes) choose what to relay and mine based on **policy** and fees — not every consensus-valid transaction is welcome under default policy.

Complete **Step 1 // Required reading** in the study plan above, then continue here. This lesson closes the **Bitcoin Basics** chapter.

## Step 2 — Core idea: auction, policy, bumping

Users experience Bitcoin through confirmation delays and fee spikes. Wallets that cannot bump fees create support nightmares.

**Analogy:** An auction-house lobby. Anyone can submit a bid (transaction). Higher fee rates tend to get seated (mined) first when space is scarce. Some items are rejected at the door for policy reasons even if they are “legal.”

```typescript
const feeRateSatVb = feeSats / vsize;
```

Estimate using node RPCs (`estimatesmartfee`) or public observers, then expose slow/medium/fast targets in UX.

<Mermaid
  caption="Stuck transactions need fee bumping via RBF or CPFP."
  chart="flowchart LR
    Tx[Broadcast tx] --> Mempool[Node mempools]
    Mempool --> Low{Fee high enough?}
    Low -->|yes| Mine[Likely next blocks]
    Low -->|no| Stuck[Sits unconfirmed]
    Stuck --> RBF[Replace-by-fee]
    Stuck --> CPFP[Child-pays-for-parent]
    RBF --> Mine
    CPFP --> Mine"
/>

- **RBF (BIP-125)**: replace an unconfirmed tx with a higher-fee variant (signals required)
- **CPFP**: spend an unconfirmed output with a high-fee child that incentivizes mining both

<Callout type="info" title="Policy ≠ consensus">
  A transaction can be consensus-valid yet non-standard under default mempool policy.
  Test relay behavior, not only block validity on regtest miner shortcuts.
</Callout>

**Done when:** You can explain when RBF vs CPFP is the right bump tool.

## Step 3 — Try it: fee math + bump choice (lab)

```bash
bitcoin-cli -regtest estimatesmartfee 6
# Or after a send: feeSats / vsize from getrawtransaction ... true
```

Pick a stuck-payment scenario (e.g. low fee, no RBF signal, or you control a change output). Write which bump you would use and why.

### Common mistakes

- Broadcasting without RBF signaling when users may need to bump
- Overpaying dramatically due to stale fee estimates
- Creating long unconfirmed chains that hit ancestor/descendant limits

**Done when:** Lab evidence (fee calc + RBF/CPFP choice + source link) is complete. Complete the **chapter quiz** when prompted.

### Next lesson

**P2P Network** (Protocol Depth) — how transactions and blocks actually propagate.
