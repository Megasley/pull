---
title: Regtest Development
description: "Run a private chain, mine blocks, and automate integration tests."
difficulty: beginner
duration: 5 days
objectives:
  - Spin up a disposable regtest environment
  - Mine blocks and fund test wallets quickly
  - Encode integration tests around deterministic scenarios
resources:
  - title: Bitcoin Core regtest mode
    url: "https://developer.bitcoin.org/examples/testing.html"
    kind: docs
    required: true
  - title: "Bitcoin Optech: Testing"
    url: "https://bitcoinops.org/en/topics/testing/"
    kind: article
    required: true
  - title: Polar (Lightning regtest UI)
    url: "https://lightningpolar.com/"
    kind: tool
    note: Useful once you reach Lightning roadmaps.
reflectionPrompts:
  - >-
    Explain Regtest Development to a teammate without jargon - what problem does
    it solve?
  - What would break in production if you misunderstood Regtest Development?
  - >-
    Which BIP, book chapter, or Core doc is authoritative here, and what did you
    verify?
lab:
  title: Regtest check
  description: >-
    Complete Steps 2–4 on a local regtest node (or Polar). Capture command
    output showing a funded wallet after mining maturity blocks.
  evidence:
    - Command output or screenshot from your local lab (createwallet / generatetoaddress)
    - One sentence on what you observed vs expected
    - Note whether you used native bitcoind, Docker, or Polar
---

**Regtest** is a private Bitcoin network you control. Difficulty is trivial, so you can mine blocks instantly without testnet faucets.

Complete **Step 1 // Required reading** in the study plan above, then continue here. Prefer local `bitcoind -regtest` or Docker; Polar is optional under **further reading**.

## Step 2 — Key terms

| Term                           | What it means                        | Where you use it                 |
| ------------------------------ | ------------------------------------ | -------------------------------- |
| **Regtest**                    | Private chain; you mine on demand    | Local labs                       |
| **Testnet / signet**           | Shared public test networks          | Occasional interop               |
| **Mainnet**                    | Real value                           | Not for learning loops           |
| **`bitcoind` / `bitcoin-cli`** | Core daemon + RPC client             | Run and drive the node           |
| **Coinbase maturity**          | Coinbase needs **100** confirmations | Why first mine uses `101` blocks |
| **Datadir**                    | Chain state on disk                  | Wipe between experiments         |

**Analogy:** Regtest is a flight simulator. Crashes are free.

**Done when:** You can explain maturity in one sentence.

## Step 3 — Setup and verification

**Option A — Native Core:** install from [Bitcoin Core releases](https://bitcoincore.org/en/download/), then:

```bash
bitcoind -regtest -daemon
bitcoin-cli -regtest getblockchaininfo
```

**Expected:** JSON with `"chain": "regtest"`.

**Option B — Docker:** fixed RPC ports, disposable volume.  
**Option C — Polar:** UI for a local network — same ideas apply.

<Callout type="tip" title="Automate resets">
  CI should use fresh datadirs. Leftover wallets and conflicting ports cause flaky
  tests.
</Callout>

**Done when:** RPC (or Polar) answers on a live regtest node.

## Step 4 — Mine, fund, confirm + lab

```bash
bitcoin-cli -regtest createwallet "alice"
ADDR=$(bitcoin-cli -regtest getnewaddress)
bitcoin-cli -regtest generatetoaddress 101 "$ADDR"  # mature coinbase
bitcoin-cli -regtest sendtoaddress <bob> 10
bitcoin-cli -regtest generatetoaddress 1 "$ADDR"
```

<Mermaid
  caption="Deterministic setup → act → confirm → assert → wipe."
  chart="flowchart TD
    Start[Start bitcoind regtest] --> Wallet[Create wallets]
    Wallet --> Mine[Mine maturity blocks]
    Mine --> Act[Send / contract actions]
    Act --> Confirm[Mine confirmations]
    Confirm --> Assert[Assert balances / txs]
    Assert --> Tear[Reset datadir]"
/>

### Common mistakes

- Not mining 100+ blocks before spending coinbase
- Shared nodes without isolation
- Hard-coding txids from a previous run

Capture **Regtest check** lab evidence (commands, observation, native/Docker/Polar).

**Done when:** You have a funded wallet you created yourself and lab evidence is filed.

### Next lesson

With a funded regtest wallet working, move on to **Git & Open Source Workflow**.
